# Moved — see `naming-conventions.md`

This document was renamed to [`naming-conventions.md`](naming-conventions.md) (2026-09-01): its scope grew
beyond servers to cover accounts, groups, cloud identity objects (Entra ID / Intune) and Azure resources,
so the old filename no longer described its contents. Section numbers are unchanged — every existing
`§N` cross-reference in this repository still points at the same section, just under the new filename.

This stub exists only because the file couldn't be deleted at the time of the rename; delete it once
every link below has been confirmed to resolve to the new file instead.

---

*Part of [UnreadLines Labs](https://youtube.com/@unreadlineslabs) — real-world enterprise infrastructure, identity, and security labs, documented the way nobody else bothers to.*
